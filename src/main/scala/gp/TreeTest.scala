package main.scala.gp

import org.jgap.gp.impl.GPConfiguration
import org.jgap.gp.impl.GPGenotype
import org.jgap.gp.terminal.Terminal
import org.jgap.gp.CommandGene
import org.jgap.util.ICloneable
import org.jgap.gp.IMutateable
import org.jgap.gp.impl.ProgramChromosome
import org.jgap.gp.function.IfElse

object TreeTest extends App {
  
  var conf = new GPConfiguration
  conf.setMaxInitDepth(5)
  
  conf.setPopulationSize(100)
  
  
  
  
//  def createGenotype(conf: GPConfiguration): GPGenotype = {
//    
//    
//    
//    //val term = new Terminal(conf, CommandGene.IntegerClass, 0.0, 11.0, true)
//    Nil
//    
//    
//  }
  
    
  
  
}
